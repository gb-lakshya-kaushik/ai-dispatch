# OpenAI API key for GPT-5 dispatch/copilot calls (optional - fallback works without it)
OPENAI_API_KEY=

# Optional model override (defaults to gpt-5)
# OPENAI_MODEL=gpt-5

# Database (defaults to SQLite in backend/data/dispatch.db)
# DATABASE_URL=sqlite:///./data/dispatch.db
